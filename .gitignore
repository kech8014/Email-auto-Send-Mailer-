node_modules
.vercel

# Real secrets never get committed; the annotated template does.
# Order matters: the negation must come after the pattern it exempts.
.env*
!.env.example

# Vendored AI-tooling docs dropped in by the Upstash installer; not app code.
.agents/
.claude/
skills-lock.json

# Throwaway diagnostic scripts.
*.tmp.js
